<h1>Coupons Index</h1>

<ul>
  <% @coupons.each do |coupon| %>
    <li><%= link_to coupon.coupon_code, coupon_path(coupon) %>|<%= coupon.store %></li>
  <% end %>
  <%= link_to 'New Coupon', new_coupon_path %>

</ul>
